# Express API URL
# For development, use your local IP address (find with: ipconfig getifaddr en0 on macOS)
NEXT_PUBLIC_API_URL=http://YOUR_LOCAL_IP:3001

# Capacitor Configuration
# Set to 'dev' to enable development mode with hot reload, or 'prod' for production builds
CAPACITOR_MODE=dev
# Development server URL (should match your local IP and Next.js dev server port)
CAPACITOR_DEV_SERVER_URL=http://YOUR_LOCAL_IP:3000
# App ID for iOS and Android (reverse domain notation)
CAPACITOR_APP_ID=os.mieweb.timeharbor
# App Name displayed on device
CAPACITOR_APP_NAME=TimeHarbor

# Firebase Cloud Messaging (Optional - for web push notifications only)
# Not required for mobile apps - they use native push via Capacitor
# NEXT_PUBLIC_FIREBASE_VAPID_KEY=your-vapid-key-here
